% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{layout_template}
\alias{layout_template}
\alias{rail_twelfths}
\title{A canonical view layout}
\usage{
layout_template(
  columns = c(filters = 2, main = 6),
  rail_position = "right",
  rail_size = 600,
  rail_collapsed_size = 35,
  rail_collapsed = FALSE
)

rail_twelfths(twelfths, viewport = 1600)
}
\arguments{
\item{columns}{Named numeric, the splitview columns in twelfths. Names are
for reading; only the ratios are used.}

\item{rail_position}{Edge the rail pins to, or \code{NULL} for no rail.}

\item{rail_size, rail_collapsed_size}{Rail width in pixels, open and collapsed.}

\item{rail_collapsed}{Whether the rail opens collapsed.}

\item{twelfths, viewport}{For \code{rail_twelfths()}, the share and the viewport
width to convert against.}
}
\value{
A \code{layout_template}.
}
\description{
The shape every view of a default board is meant to have: some columns in
the splitview, and a rail pinned to one edge. \code{columns} is in twelfths, the
way you would say it out loud ("two for filters, six for content"); what a
grid stores is ratios, so they are normalised.
}
\details{
The rail is in pixels, not twelfths, because that is what a \code{dock_grid}
stores: a rail sits outside the splitview and does not take a share of it.
\code{rail_twelfths()} converts against an assumed viewport if you would rather
state it the same way as the columns.
}
\examples{
# two columns of filters, six of content, four for the rail on a 1600px
# viewport
layout_template(
  columns = c(filters = 2, main = 6),
  rail_size = rail_twelfths(4)
)

}
