% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/board-script.R
\name{board_script}
\alias{board_script}
\alias{write_board_script}
\title{Board construction source}
\usage{
board_script(
  x,
  name = "board",
  rename = TRUE,
  tidy = TRUE,
  group = rename,
  ref_fields = default_ref_fields()
)

write_board_script(
  x,
  file,
  name = "board",
  rename = TRUE,
  tidy = TRUE,
  group = rename,
  ref_fields = default_ref_fields(),
  header = NULL
)
}
\arguments{
\item{x}{A board, or the list \code{blockr.core::blockr_ser()} produces for one.}

\item{name}{Name of the function the script defines.}

\item{rename}{Slug the block and view ids. \code{FALSE} keeps the stored ids,
which is what you want when comparing generated source against a stored
spec byte for byte.}

\item{tidy}{Collapse duplicate state entries. \code{FALSE} reproduces the stored
payload verbatim.}

\item{group}{Order the blocks by the view that first shows them, under a
section comment. Defaults to \code{rename}, since both are readability moves;
set it \code{FALSE} to keep the stored order.}

\item{ref_fields}{Payload field names that hold a block id, and so have to be
rewritten when ids are renamed. Only these are touched: a payload value
that merely coincides with an id (a \code{head} block stores
\code{direction = "head"}) must not be rewritten. Any stored id the rename
leaves behind raises a warning naming the field, which is how you find a
block type with a reference field this does not know about.}

\item{file}{Path to write to.}

\item{header}{Comment lines placed above the function. \code{NULL} writes a
default header naming the board and the counts.}
}
\value{
A single string: the source of a function that returns the board.
}
\description{
\code{board_script()} turns a saved board into the R script that rebuilds it. A
board's JSON is a serialization of the constructor calls that produced it --
every object records its constructor name, its package and a payload of
literals -- so the two forms carry the same information and the conversion is
mechanical. What this adds over a \code{dput()} is readability:
}
\details{
\itemize{
\item block ids become slugs of \code{block_name} (\code{global_population_filter} rather
than \code{bsifddnw}), and every reference moves with them: links, view members,
grid panels, stack members, and the payload fields blocks use to point at
each other
\item view ids likewise become slugs of the view name, carried into the \code{grids}
keys and \code{active}
\item duplicate \code{visible.N} state entries are collapsed (see \code{collapse_state()}
for why they are not merely noise)
\item a code block's \code{script} is emitted as a raw string literal, so it reads as
the code it is while still round-tripping to the stored line vector
\item blocks are grouped under the view that first shows them
}

}
\examples{
\dontrun{
board <- blockr.core::blockr_deser(
  jsonlite::fromJSON("board.json", simplifyVector = FALSE)
)
cat(board_script(board, "my_board"))
}

}
