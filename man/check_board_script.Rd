% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roundtrip.R
\name{check_board_script}
\alias{check_board_script}
\title{Check that a board survives the round trip}
\usage{
check_board_script(x, rename = TRUE, tidy = TRUE)
}
\arguments{
\item{x}{A board, or the list \code{blockr.core::blockr_ser()} produces for one.}

\item{rename}{Slug the block and view ids. \code{FALSE} keeps the stored ids,
which is what you want when comparing generated source against a stored
spec byte for byte.}

\item{tidy}{Collapse duplicate state entries. \code{FALSE} reproduces the stored
payload verbatim.}
}
\value{
A list with \code{ok} and one entry per compared component. Printed as a
short report.
}
\description{
Generates the construction source for a board, evaluates it, and compares
the result against the original. This is the point of the package: run it
over every board you store and it fails the day one starts carrying state
that no constructor argument can restore.
}
\details{
With \code{rename = FALSE} and \code{tidy = FALSE} the comparison is exact: the
re-serialized board must be \code{identical()} to the stored one, component by
component. The one allowance is \code{focus}, which the generated script never
emits (see \code{grid_src()}), so it is stripped from both sides before the grids
are compared. With the defaults the ids have changed on purpose, so the check
is equivalence instead -- same block set, same payloads once ids are
remapped, same link topology, same view membership.
}
\examples{
\dontrun{
res <- check_board_script(board)
stopifnot(res$ok)
}

}
