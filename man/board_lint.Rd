% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lint.R
\name{board_lint}
\alias{board_lint}
\title{Defects in a stored board}
\usage{
board_lint(x)
}
\arguments{
\item{x}{A board, or the list \code{blockr.core::blockr_ser()} produces for one.}
}
\value{
A data frame with \code{kind}, \code{id} and \code{detail}, one row per finding.
Zero rows is the good case. Printed as a short report.
}
\description{
A board file accumulates. Blocks are saved with whatever state they were last
left in, and some of what lands there is wrong in ways nothing reports at
runtime. \code{board_lint()} names them.
}
\details{
The checks, each grounded in something found on a real board:

\describe{
\item{\code{ambiguous_visible}}{Several \code{visible.N} payload entries and no exact
\code{visible}. \code{attr()} partial-matches, so several candidates make the lookup
ambiguous, \code{attr()} returns \code{NULL}, and the block silently falls back to
showing every section. The setting is lost with no error anywhere. Not
every one of these looks wrong: the fallback sometimes happens to be what
the block wanted. On the board this was found on, 32 blocks were
ambiguous and 13 rendered differently because of it. \code{\link[=board_script]{board_script()}}
with \code{tidy = TRUE} writes the repaired value.}
\item{\code{suffixed_state}}{One suffixed entry and no exact name, so the value
is reached only by \code{attr()} partial matching. It works, until the next
save adds a second one and it does not. \code{\link[=normalize_state]{normalize_state()}} renames it.}
\item{\code{duplicate_state}}{Payload entries duplicated by base name, without
the ambiguity above. Harmless but not free: on one board 54\% of all
entries, and every one of them lands in the file and in the diff.}
\item{\code{unexported_ctor}}{The recorded constructor is not exported by its
package. Deserialization reaches it anyway (\code{get0()} on the namespace),
so this never fails at load; it means a constructor a board can store is
not part of any package's API, and generated source has to use \code{:::}.}
\item{\code{serialized_ctor}}{The constructor is stored as a serialized closure
rather than a name and a package: kilobytes of R bytecode in the file,
pinned to the R version that wrote it, and unreadable in a diff.}
\item{\code{non_literal_payload}}{A payload value that is a function,
environment or raw vector. Everything else is a literal, so this is the
shape that cannot be written back as source.}
}
}
\examples{
\dontrun{
issues <- board_lint(read_board("board.json"))
subset(issues, kind == "ambiguous_visible")
}

}
