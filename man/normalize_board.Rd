% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/payload.R
\name{normalize_board}
\alias{normalize_board}
\title{Put a board back on its template}
\usage{
normalize_board(x, template = layout_template(), even_nested = TRUE)
}
\arguments{
\item{x}{A board, or the list \code{blockr.core::blockr_ser()} produces for one.}

\item{template}{A \code{\link[=layout_template]{layout_template()}}.}

\item{even_nested}{Even out the sizes of branches nested inside a column.
The template says nothing about those, so they are drift by the same
argument, but set this \code{FALSE} to leave them.}
}
\value{
A board.
}
\description{
\code{\link[=normalize_layout]{normalize_layout()}} and \code{\link[=normalize_state]{normalize_state()}} in one call: the shape a default
board is meant to have, and the state it is meant to carry. Nothing else
moves. No block changes column, no panel joins or leaves a view, no
configuration is touched.
}
\examples{
\dontrun{
clean <- normalize_board(board, layout_template(c(filters = 2, main = 6)))
}

}
